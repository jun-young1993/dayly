<?xml version="1.0" encoding="utf-8"?>
<!--
  Small 위젯 컨테이너 (2x2 셀 기준)
  StackView로 여러 D-Day 이벤트를 스와이프 탐색할 수 있다.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="4dp"
    android:clipChildren="true"
    android:clipToPadding="true">

    <StackView
        android:id="@+id/widget_stack"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:loopViews="true" />

    <!-- 이벤트가 없을 때 표시되는 빈 상태 뷰 (탭 시 앱 실행) -->
    <LinearLayout
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/dayly_widget_bg"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="dayly"
            android:textColor="#4060A0"
            android:textSize="13sp"
            android:fontFamily="monospace" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="+ 추가"
            android:textColor="#2A3A5A"
            android:textSize="9sp" />

    </LinearLayout>

</FrameLayout>
